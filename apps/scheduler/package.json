{
  "name": "@sentinelqa/scheduler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@sentinelqa/db": "workspace:*",
    "@sentinelqa/dsl": "workspace:*",
    "bullmq": "^5.76.5",
    "cron-parser": "^5.5.0",
    "ioredis": "^5.10.1"
  },
  "devDependencies": {
    "typescript": "^6.0.3"
  }
}
